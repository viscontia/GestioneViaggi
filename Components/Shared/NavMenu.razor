@using GestioneViaggi.Services.Authentication
@using GestioneViaggi.Services.Navigation
@using GestioneViaggi.Services.Printing
@using Microsoft.AspNetCore.Components.Authorization
@inject IAuthenticationService AuthService

@inject NavigationManager Navigation
@inject ITabManagerService TabManager
@inject ISnackbar Snackbar
@inject IDialogService DialogService
@inject ITravelPrintService PrintService
@inject IRoomingListPrintService RoomingListPrintService

<MudNavMenu>
    <MudNavLink Icon="@Icons.Material.Filled.Dashboard"
                OnClick="@(() => OpenTab(_dashboardRoute, "Dashboard", "dashboard"))">
        <MudText Typo="Typo.body1">Dashboard</MudText>
    </MudNavLink>

    @* MULTI-TENANT: Tutti vedono menu Aziende (SuperAdmin: tutte | Altri: solo propria) *@
    <MudNavGroup Title="Anagrafiche" Icon="@Icons.Material.Filled.AccountBox" Expanded="false">
        <MudNavLink Icon="@Icons.Material.Filled.Business"
                    OnClick="@(() => OpenTab("/anagrafiche/aziende", "Anagrafica Aziende", "business"))">
            <MudText Typo="Typo.body1">Anagrafica Aziende</MudText>
        </MudNavLink>

        <MudNavLink Icon="@Icons.Material.Filled.People"
                    OnClick="@(() => OpenTab("/anagrafiche/clienti", "Anagrafica Clienti", "people"))">
            <MudText Typo="Typo.body1">Anagrafica Clienti</MudText>
        </MudNavLink>

        <MudNavLink Icon="@Icons.Material.Filled.Store"
                    OnClick="@(() => OpenTab("/ana-controparti", "Anagrafica Controparti", "store"))">
            <MudText Typo="Typo.body1">Anagrafica Controparti</MudText>
        </MudNavLink>

        <MudNavLink Icon="@Icons.Material.Filled.FlightTakeoff"
                    OnClick="@(() => OpenTab("/ana-viaggi", "Anagrafica Viaggi e Date", "flight_takeoff"))">
            <MudText Typo="Typo.body1">Anagrafica Viaggi e Date</MudText>
        </MudNavLink>
    </MudNavGroup>

    <MudNavGroup Title="Tabelle" Icon="@Icons.Material.Filled.TableChart" Expanded="false">

        <!-- Tabelle Geografiche -->
        <MudNavGroup Title="Tabelle Geografiche" Icon="@Icons.Material.Filled.Public" Expanded="false">
            <MudNavLink Icon="@Icons.Material.Filled.Public"
                        OnClick="@(() => OpenTab("/tabelle/nazioni", "Nazioni", "table"))">
                <MudText Typo="Typo.body1">Nazioni</MudText>
            </MudNavLink>

            <MudNavLink Icon="@Icons.Material.Filled.Map"
                        OnClick="@(() => OpenTab("/tabelle/regioni", "Regioni", "table"))">
                <MudText Typo="Typo.body1">Regioni</MudText>
            </MudNavLink>

            <MudNavLink Icon="@Icons.Material.Filled.LocationCity"
                        OnClick="@(() => OpenTab("/tabelle/capoluoghi", "Capoluoghi", "table"))">
                <MudText Typo="Typo.body1">Capoluogo di Regione</MudText>
            </MudNavLink>

            <MudNavLink Icon="@Icons.Material.Filled.Landscape"
                        OnClick="@(() => OpenTab("/tabelle/ripartizioni-geografiche", "Ripartizioni Geografiche", "table"))">
                <MudText Typo="Typo.body1">Ripartizioni Geografiche</MudText>
            </MudNavLink>

            <MudNavLink Icon="@Icons.Material.Filled.Terrain"
                        OnClick="@(() => OpenTab("/tabelle/province", "Province", "table"))">
                <MudText Typo="Typo.body1">Province</MudText>
            </MudNavLink>

            <MudNavLink Icon="@Icons.Material.Filled.Place"
                        OnClick="@(() => OpenTab("/tabelle/comuni", "Comuni", "table"))">
                <MudText Typo="Typo.body1">Comuni</MudText>
            </MudNavLink>
        </MudNavGroup>

        <!-- Tabelle Viaggi -->
        <MudNavGroup Title="Tabelle Viaggi" Icon="@Icons.Material.Filled.Explore" Expanded="false">
            <MudNavLink Icon="@Icons.Material.Filled.Category"
                        OnClick="@(() => OpenTab("/tabelle/tipologie-viaggio", "Tipologie Viaggio", "table"))">
                <MudText Typo="Typo.body1">Tipologie Viaggio</MudText>
            </MudNavLink>

            <MudNavLink Icon="@Icons.Material.Filled.People"
                        OnClick="@(() => OpenTab("/tabelle/tipologie-partecipanti", "Tipologie Partecipanti", "table"))">
                <MudText Typo="Typo.body1">Tipologie Partecipanti</MudText>
            </MudNavLink>

            <MudNavLink Icon="@Icons.Material.Filled.Hotel"
                        OnClick="@(() => OpenTab("/tabelle/tipologie-alloggi", "Tipologie Alloggi", "table"))">
                <MudText Typo="Typo.body1">Tipologie Alloggi</MudText>
            </MudNavLink>

            <MudNavLink Icon="@Icons.Material.Filled.Restaurant"
                        OnClick="@(() => OpenTab("/tabelle/tipologie-trattamento-alberghiero", "Tipologie Tratt. Alberghiero", "table"))">
                <MudText Typo="Typo.body1">Tipologie Tratt. Alberghiero</MudText>
            </MudNavLink>

            <MudNavLink Icon="@Icons.Material.Filled.NightsStay"
                        OnClick="@(() => OpenTab("/tabelle/tipologie-pernottamento", "Tipologie Pernottamento", "table"))">
                <MudText Typo="Typo.body1">Tipologie di Pernottamento</MudText>
            </MudNavLink>

            <MudNavLink Icon="@Icons.Material.Filled.Commute"
                        OnClick="@(() => OpenTab("/tabelle/tipologie-avvicinamento", "Tipologie Avvicinamento", "table"))">
                <MudText Typo="Typo.body1">Tipologie Avvicinamento</MudText>
            </MudNavLink>
        </MudNavGroup>

        <!-- Tabelle Mezzi -->
        <MudNavGroup Title="Tabelle Mezzi" Icon="@Icons.Material.Filled.DirectionsCar" Expanded="false">
            <MudNavLink Icon="@Icons.Material.Filled.CarRental"
                        OnClick="@(() => OpenTab("/tabelle/marche-veicoli", "Marche Veicoli", "table"))">
                <MudText Typo="Typo.body1">Marche Veicoli</MudText>
            </MudNavLink>

            <MudNavLink Icon="@Icons.Material.Filled.DirectionsCar"
                        OnClick="@(() => OpenTab("/tabelle/mezzi-modelli", "Modelli Veicoli", "table"))">
                <MudText Typo="Typo.body1">Modelli Veicoli</MudText>
            </MudNavLink>

            <MudNavLink Icon="@Icons.Material.Filled.Category"
                        OnClick="@(() => OpenTab("/tabelle/tipologie-veicoli", "Tipologie Veicoli", "table"))">
                <MudText Typo="Typo.body1">Tipologie Veicoli</MudText>
            </MudNavLink>
        </MudNavGroup>

        <!-- Tabelle Contabili -->
        <MudNavGroup Title="Tabelle Contabili" Icon="@Icons.Material.Filled.Calculate" Expanded="false">
             <MudNavLink Icon="@Icons.Material.Filled.Category"
                        OnClick="@(() => OpenTab("/tabelle/tipi-fornitore", "Tipi Controparti", "table"))">
                <MudText Typo="Typo.body1">Tipi Controparti</MudText>
            </MudNavLink>
             <MudNavLink Icon="@Icons.Material.Filled.AttachMoney"
                        OnClick="@(() => OpenTab("/tabelle/valute", "Valute", "table"))">
                <MudText Typo="Typo.body1">Valute</MudText>
            </MudNavLink>
             <MudNavLink Icon="@Icons.Material.Filled.Timeline"
                        OnClick="@(() => OpenTab("/tabelle/tassi-cambio", "Storico Tassi", "table"))">
                <MudText Typo="Typo.body1">Storico Tassi</MudText>
            </MudNavLink>
             <MudNavLink Icon="@Icons.Material.Filled.FactCheck"
                        OnClick="@(() => OpenTab("/tabelle/causali", "Causali", "table"))">
                <MudText Typo="Typo.body1">Causali</MudText>
            </MudNavLink>
             <MudNavLink Icon="@Icons.Material.Filled.Percent"
                        OnClick="@(() => OpenTab("/tabelle/aliquote-iva", "Aliquote IVA", "table"))">
                <MudText Typo="Typo.body1">Aliquote IVA</MudText>
            </MudNavLink>
        </MudNavGroup>

    </MudNavGroup>



    <MudNavGroup Title="Contabilità" Icon="@Icons.Material.Filled.AttachMoney" Expanded="false">
        <MudNavLink Icon="@Icons.Material.Filled.ReceiptLong"
                    OnClick="@(() => OpenTab("/movimenti", "Movimenti Contabili", "receipt_long"))">
            <MudText Typo="Typo.body1">Movimenti Contabili</MudText>
        </MudNavLink>
    </MudNavGroup>

    <MudNavGroup Title="Stampe Viaggi" Icon="@Icons.Material.Filled.Print" Expanded="false">
        <MudNavLink Icon="@Icons.Material.Filled.Description"
                    OnClick="@OpenStampaSchedaDataViaggio">
            <MudText Typo="Typo.body1">Stampa Scheda Data Viaggio</MudText>
        </MudNavLink>
        <MudNavLink Icon="@Icons.Material.Filled.Description"
                    OnClick="@OpenStampaDettaglioDataViaggio">
            <MudText Typo="Typo.body1">Stampa Dettaglio Data Viaggio</MudText>
        </MudNavLink>
        <MudNavLink Icon="@Icons.Material.Filled.Hotel"
                    OnClick="@OpenStampaRoomingList">
            <MudText Typo="Typo.body1">Stampa Rooming List</MudText>
        </MudNavLink>
    </MudNavGroup>

    <MudNavGroup Title="Stampe Contabili" Icon="@Icons.Material.Filled.AccountBalance" Expanded="false">
        <MudNavLink Icon="@Icons.Material.Filled.Receipt"
                    OnClick="@OpenStampaMovimentiDialog">
            <MudText Typo="Typo.body1">Stampa Movimenti Contabili</MudText>
        </MudNavLink>
        <MudNavLink Icon="@Icons.Material.Filled.EventNote"
                    OnClick="@OpenStampaScadenzarioDialog">
            <MudText Typo="Typo.body1">Scadenziario</MudText>
        </MudNavLink>
        <MudNavLink Icon="@Icons.Material.Filled.AccountBalanceWallet"
                    OnClick="@OpenStampaBilancioViaggioDialog">
            <MudText Typo="Typo.body1">Stampa Bilancio Viaggio/i</MudText>
        </MudNavLink>
    </MudNavGroup>



    <AuthorizeView Roles="superadmin">
        <MudNavGroup Title="Amministrazione" Icon="@Icons.Material.Filled.AdminPanelSettings" Expanded="false">
             <MudNavLink Icon="@Icons.Material.Filled.People"
                        OnClick="@(() => OpenTab("/amministrazione/users", "Gestione Utenti", "people"))">
                <MudText Typo="Typo.body1">Gestione Utenti</MudText>
            </MudNavLink>
             <MudNavLink Icon="@Icons.Material.Filled.Badge"
                        OnClick="@(() => OpenTab("/amministrazione/roles", "Gestione Ruoli", "badge"))">
                <MudText Typo="Typo.body1">Gestione Ruoli</MudText>
            </MudNavLink>
        </MudNavGroup>

        <MudNavGroup Title="Utilità" Icon="@Icons.Material.Filled.Build" Expanded="false">
            <MudNavLink Icon="@Icons.Material.Filled.ImportExport"
                        OnClick="@(() => OpenTab("/tools/oracle-import", "Importazione Dati da Oracle", "import_export"))">
                <MudText Typo="Typo.body1">Importazione Dati da Oracle</MudText>
            </MudNavLink>
             <MudNavLink Icon="@Icons.Material.Filled.Description"
                        OnClick="@(() => OpenTab("/tools/db-documentation", "Documentazione DB", "description"))">
                <MudText Typo="Typo.body1">Documentazione DB</MudText>
            </MudNavLink>
             <MudNavLink Icon="@Icons.Material.Filled.CleaningServices"
                        OnClick="@(() => OpenTab("/tools/event-maintenance", "Manutenzione Eventi", "cleaning_services"))">
                <MudText Typo="Typo.body1">Manutenzione Eventi</MudText>
            </MudNavLink>
        </MudNavGroup>
    </AuthorizeView>

    <MudDivider Class="my-2" />

    <div class="mud-nav-item" @onclick="HandleLogout" style="cursor: pointer; display: flex; align-items: center; padding: 8px 16px;">
        <MudIcon Icon="@Icons.Material.Filled.Logout" Size="Size.Medium" Class="mr-3" />
        <MudText Typo="Typo.body1">Logout</MudText>
    </div>
</MudNavMenu>

@code {
    private string _dashboardRoute = "/dashboard-admin";

    protected override async Task OnInitializedAsync()
    {
        var user = await AuthService.GetCurrentUserAsync();
        _dashboardRoute = user?.IsSuperAdmin == true
            ? "/dashboard-superadmin"
            : "/dashboard-admin";
    }

    private void OpenTab(string route, string title, string icon)
    {
        if (!TabManager.CanOpenTab())
        {
            Snackbar.Add($"Impossibile aprire '{title}': limite di {TabManager.MaxTabs} tab raggiunto. Chiudi una tab per continuare.", Severity.Warning, config =>
            {
                config.VisibleStateDuration = 4000;
            });
            return;
        }

        bool opened = TabManager.OpenTab(route, title, icon);
        if (opened)
        {
            Snackbar.Add($"Tab aperto: {title}", Severity.Success, config =>
            {
                config.VisibleStateDuration = 2000;
            });
        }
    }

    private async Task HandleLogout()
    {
        // Perform logout - force reload will reset everything
        await AuthService.LogoutAsync();

        // Force reload to login page (this will destroy all components and tabs)
        Navigation.NavigateTo("/login", forceLoad: true);
    }

    private async Task OpenStampaSchedaDataViaggio()
    {
        var dialog = await DialogService.ShowAsync<SelectDataViaggioDialog>("Seleziona Data Viaggio da Stampare", new DialogOptions
        {
            MaxWidth = MaxWidth.Medium,
            FullWidth = true
        });

        var result = await dialog.Result;

        if (result != null && !result.Canceled && result.Data is int dataViaggioId)
        {
            try
            {
                var data = await PrintService.GetPrintDataAsync(dataViaggioId);

                // STANDARD: Save to Downloads
                var targetFolder = Path.Combine(Environment.GetFolderPath(Environment.SpecialFolder.UserProfile), "Downloads");

                // Create descriptive filename
                var fileName = GestioneViaggi.Services.Printing.PdfFileNameHelper.GetTravelSheetFileName(data.Header);
                var filePath = Path.Combine(targetFolder, fileName);

                // STANDARD: Overwrite check
                if (System.IO.File.Exists(filePath))
                {
                    System.IO.File.Delete(filePath);
                }

                await GestioneViaggi.Services.Printing.ViaggiPrinter.GeneratePdfAsync(data, filePath);

                // STANDARD: Confirmation and Open
                bool? confirm = await DialogService.ShowMessageBox(
                    "Stampa Completata",
                    $"Il file è stato salvato in Downloads:\n{fileName}\n\nVuoi aprirlo?",
                    yesText: "Sì, Apri", cancelText: "No");

                if (confirm == true)
                {
                    // STANDARD: Cross-Platform Open
#if MACCATALYST
                    System.Diagnostics.Process.Start("open", $"\"{filePath}\"");
#else
                    await Launcher.Default.OpenAsync(new OpenFileRequest
                    {
                        Title = "Scheda Viaggio",
                        File = new ReadOnlyFile(filePath)
                    });
#endif
                }
            }
            catch (Exception ex)
            {
                Snackbar.Add($"Errore stampa: {ex.Message}", Severity.Error);
            }
        }
    }

    private async Task OpenStampaDettaglioDataViaggio()
    {
        var dialog = await DialogService.ShowAsync<SelectDataViaggioDialog>("Seleziona Data Viaggio (Stampa Dettagliata)", new DialogOptions
        {
            MaxWidth = MaxWidth.Medium,
            FullWidth = true
        });

        var result = await dialog.Result;

        if (result != null && !result.Canceled && result.Data is int dataViaggioId)
        {
            try
            {
                var data = await PrintService.GetPrintDataAsync(dataViaggioId);

                // STANDARD: Save to Downloads
                var targetFolder = Path.Combine(Environment.GetFolderPath(Environment.SpecialFolder.UserProfile), "Downloads");

                // Create descriptive filename
                var fileName = GestioneViaggi.Services.Printing.PdfFileNameHelper.GetTravelSheetFileName(data.Header, detailed: true);
                var filePath = Path.Combine(targetFolder, fileName);

                // STANDARD: Overwrite check
                if (System.IO.File.Exists(filePath))
                {
                    System.IO.File.Delete(filePath);
                }

                await GestioneViaggi.Services.Printing.ViaggiPrinter.GenerateDetailedPdfAsync(data, filePath);

                // STANDARD: Confirmation and Open
                bool? confirm = await DialogService.ShowMessageBox(
                    "Stampa Completata",
                    $"Il file è stato salvato in Downloads:\n{fileName}\n\nVuoi aprirlo?",
                    yesText: "Sì, Apri", cancelText: "No");

                if (confirm == true)
                {
                    // STANDARD: Cross-Platform Open
#if MACCATALYST
                    System.Diagnostics.Process.Start("open", $"\"{filePath}\"");
#else
                    await Launcher.Default.OpenAsync(new OpenFileRequest
                    {
                        Title = "Scheda Dettaglio",
                        File = new ReadOnlyFile(filePath)
                    });
#endif
                }
            }
            catch (Exception ex)
            {
                Snackbar.Add($"Errore stampa: {ex.Message}", Severity.Error);
            }
        }
    }

    private async Task OpenStampaRoomingList()
    {
        var dialog = await DialogService.ShowAsync<SelectDataViaggioDialog>("Seleziona Data Viaggio per Rooming List", new DialogOptions
        {
            MaxWidth = MaxWidth.Medium,
            FullWidth = true
        });

        var result = await dialog.Result;

        if (result != null && !result.Canceled && result.Data is int dataViaggioId)
        {
            try
            {
                var data = await RoomingListPrintService.GetRoomingListDataAsync(dataViaggioId);

                // STANDARD: Save to Downloads
                var targetFolder = Path.Combine(Environment.GetFolderPath(Environment.SpecialFolder.UserProfile), "Downloads");

                // Create descriptive filename
                var fileName = GestioneViaggi.Services.Printing.PdfFileNameHelper.GetRoomingListFileName(data.Header);
                var filePath = Path.Combine(targetFolder, fileName);

                // STANDARD: Overwrite check
                if (System.IO.File.Exists(filePath))
                {
                    System.IO.File.Delete(filePath);
                }

                await GestioneViaggi.Services.Printing.RoomingListPrinter.GeneratePdfAsync(data, filePath);

                // STANDARD: Confirmation and Open
                bool? confirm = await DialogService.ShowMessageBox(
                    "Stampa Completata",
                    $"Il file è stato salvato in Downloads:\n{fileName}\n\nVuoi aprirlo?",
                    yesText: "Sì, Apri", cancelText: "No");

                if (confirm == true)
                {
                    // STANDARD: Cross-Platform Open
#if MACCATALYST
                    System.Diagnostics.Process.Start("open", $"\"{filePath}\"");
#else
                    await Launcher.Default.OpenAsync(new OpenFileRequest
                    {
                        Title = "Rooming List",
                        File = new ReadOnlyFile(filePath)
                    });
#endif
                }
            }
            catch(Exception ex)
            {
                Snackbar.Add($"Errore stampa: {ex.Message}", Severity.Error);
            }
        }
    }

    private async Task OpenStampaMovimentiDialog()
    {
        var options = new DialogOptions
        {
            CloseOnEscapeKey = true,
            MaxWidth = MaxWidth.Large,
            FullWidth = true,
            BackdropClick = false
        };
        await DialogService.ShowAsync<StampaMovimentiDialog>("Stampa Movimenti Contabili", options);
    }

    private async Task OpenStampaScadenzarioDialog()
    {
        var options = new DialogOptions
        {
            CloseOnEscapeKey = true,
            MaxWidth = MaxWidth.Large,
            FullWidth = true,
            BackdropClick = false
        };
        await DialogService.ShowAsync<StampaScadenzarioDialog>("Stampa Scadenzario", options);
    }

    private async Task OpenStampaBilancioViaggioDialog()
    {
        var options = new DialogOptions
        {
            CloseOnEscapeKey = true,
            MaxWidth = MaxWidth.Medium,
            FullWidth = true,
            BackdropClick = false
        };
        await DialogService.ShowAsync<StampaBilancioViaggioDialog>("Stampa Bilancio Viaggio", options);
    }
}
