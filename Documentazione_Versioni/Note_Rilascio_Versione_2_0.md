# Note di Rilascio — Versione 2.0

> Salto maggiore **1.35 → 2.0**: introduzione dell'**Estensione CMS per il Web** nel gestionale, più una serie di bug pre-esistenti individuati e risolti durante i test di pre-rilascio.
> Data: 2026-07-19 · ultimo aggiornamento 2026-08-20. Stato: in preparazione (documento vivo fino al go-live).

---

## Sezione 1 — Nuove funzionalità: Estensione CMS per il Web

Il gestionale diventa l'**unico motore di contenuti** per il nuovo sito pubblico: i contenuti web sono redatti e gestiti dal gestionale (nessun CMS separato) e pubblicati verso il sito.

> **Criterio di inserimento:** in questa sezione entrano **solo le funzionalità verificate a runtime**. Le altre restano nell'elenco "In attesa di verifica" finché non sono collaudate, e vengono promosse a funzionalità rilasciata una volta testate.

### Funzionalità verificate

- **Lingua del cliente (anagrafica)** — nuovo campo *lingua preferita* in anagrafica cliente, usato dalla newsletter per l'invio multilingua. Il campo non è mai vuoto: se l'operatore non lo specifica, la lingua viene **auto-derivata dalla nazione di residenza** (fallback italiano) e memorizzata; la newsletter la legge senza dover ragionare. *(Verificato a runtime: 2026-07-19.)*
- **Tipi viaggio globali + descrizioni web** — lookup condivisa fra le aziende, con mappatura tipo → descrizione web e relative traduzioni. *(Verificato a runtime: 2026-07-25.)*
- **Schede web dei tour per edizione (viaggio + data)** — un contenuto per singola partenza, con creazione dalla scheda viaggio e **anteprima** di come apparirà sul sito; l'anteprima riporta le **prossime partenze in programma** e può essere visualizzata **in lingua** quando le traduzioni sono complete e approvate. *(Verificato a runtime: 2026-07-31.)*
- **Itinerario giorno per giorno** — giornate e passi con riordino; ogni giornata mostra la **data reale derivata dalla partenza**, che si aggiorna da sola se la giornata viene spostata. *(Verificato a runtime: 2026-07-31.)*
- **Galleria immagini del tour** — caricamento con conversione WebP e archiviazione su Supabase Storage, copertina, riordino, protezione delle foto usate nell'itinerario. *(Verificato a runtime: 2026-07-31.)*
- **Mappe statiche da traccia GPX** — **più mappe per edizione**: una dell'intero viaggio e una per giornata dell'itinerario, con descrizione tradotta, blocco dei file duplicati e tracciato volutamente generalizzato perché non sia replicabile. *(Verificato a runtime: 2026-07-29.)*
- **Traduzioni assistite multilingua (Claude)** — traduzione di ciò che manca o è obsoleto, revisione con editor visuale (mai HTML a vista), **approvazione in blocco** dopo un controllo a campione per lingua, e **gating**: non si pubblica finché le traduzioni non sono revisionate. Include il **registro dei consumi** con soglia di spesa facoltativa e avviso. *(Verificato a runtime: 2026-07-31.)*
- **Incluso / Escluso per tour** — campi a livello viaggio, condivisi fra le edizioni e tradotti insieme al resto della scheda. *(Verificato a runtime: 2026-07-31.)*
- **Cifratura dei segreti (`pgcrypto`)** — chiavi salvate cifrate e rilette in chiaro solo con la master key d'ambiente. *(Verificato a runtime sulla chiave Claude: 2026-07-28. Il percorso SMTP usa lo stesso meccanismo ma non è stato riesercitato in questo ciclo.)*

### In attesa di verifica runtime (non ancora documentate come rilasciate)

Implementate ma da collaudare prima di promuoverle sopra. Le voci qui sotto **non sono state esercitate** nel ciclo di test 2026-07-25 → 08-01: per alcune manca il dato di prova, per altre serve il sito pubblico (Fase 3).

- [ ] **Clonazione** di una scheda web su un'altra edizione (creazione e anteprima già verificate)
- [ ] **Clonazione fra partenze di durata diversa**: se l'edizione di destinazione dura meno di quella di origine, il programma se ne accorge e chiede se rinunciare o clonare solo le prime giornate, avvertendo che l'ultima va rivista a mano
- [ ] **Eliminazione di una scheda web** (solo bozza o archiviata), con conferma che elenca giornate, foto, mappe e traduzioni che spariscono. Serve anche a sbloccare la cancellazione di una partenza futura
- [ ] **Il sito non mostra partenze già iniziate**: filtro sulla data di inizio in lettura — verificabile solo con il frontend pubblico (Fase 3)
- [ ] Capienza e "posti rimasti" per tour — la capienza si imposta dal gestionale, ma *posti rimasti* è un dato che espone il **sito pubblico**: verificabile solo con il frontend (Fase 3)
- [ ] Tour brevi / giornalieri — nessun tipo viaggio ancora marcato come breve
- [ ] Newsletter multilingua (invio, iscrizione/disiscrizione, destinatari) — nessun invio né iscritto sul database di prova
- [ ] Configurazione funzioni web per azienda — nessuna funzione ancora configurata
- [ ] Recensioni Google / TripAdvisor — dipende dalla configurazione funzioni web, non ancora impostata
- [ ] SMTP: test connessione con diagnostica (firewall/VPN, DNS, TLS…), messaggi errore centralizzati, mostra/nascondi password

> Nota architetturale: logica dati **DB-first** (funzioni PostgreSQL), componenti UI **condivisi**, contenuti web **per-azienda** (silos multi-tenant); solo tipi-viaggio e relative descrizioni sono globali.

---

## Sezione 2 — Bug pre-esistenti individuati e risolti

Bug non legati all'estensione web, emersi durante i test di pre-rilascio e corretti in questa versione.

| # | Area | Problema | Soluzione |
|---|------|----------|-----------|
| 1 | **Anagrafica Cliente — validazione** | I campi **obbligatori su tab non visitati** (es. "Documenti") **non bloccavano il salvataggio** (insert e update): `MudForm` valida solo i campi renderizzati e `MudTabs` renderizzava solo il pannello attivo, quindi i `Required` sugli altri tab non venivano registrati. | `KeepPanelsAlive="true"` sui `MudTabs`: tutti i pannelli renderizzati → tutti i campi validati. (Stesso schema già corretto in passato su `AziendaDialog`/`AnaViaggiDialog`; mancava su `ClienteDialog`.) |
| 2 | **Anagrafica Cliente — lingua (salvataggio)** | Il cast `@L::char` (= `char(1)`) in `ClienteLinguaService.SetAsync` **troncava** il codice lingua (`IT`→`I`) prima della funzione DB → al rientro il campo mostrava il codice grezzo. | Cast corretto a `::varchar`; script di **repair dati** per le righe già troncate (rimappa 1 carattere → ISO a 2 lettere). |
| 3 | **Anagrafica Cliente — lingua (NULL)** | Svuotando il campo lingua si salvava **NULL** in `cliente_lingua`, delegando alla newsletter la derivazione. | La funzione `fn_ana_clienti_set_lingua` **auto-deriva** la lingua dalla nazione di residenza quando il campo è vuoto (fallback `IT`); colonna resa `NOT NULL DEFAULT 'IT'`. La newsletter legge il valore senza dover ragionare. |
| 4 | **Anagrafica Cliente — layout** | La form del dialog aveva **altezza fissa (800px)** → grande spazio vuoto verticale e barra di scorrimento inutile; spaziatura eccessiva tra i campi. | Altezza resa **adattiva** (`max-height: 80vh`) e spaziatura dei tab compattata. |
| 5 | **SMTP — lettura password** | Il recupero della password SMTP in chiaro leggeva `password_enc->>'value'` (JSONB) su una colonna diventata `bytea` (pgcrypto) → errore silenzioso, il test dava sempre "password non recuperabile" anche con password salvata. | Nuova funzione DB `fn_ana_aziende_smtp_secrets_get` (decifra via `pgp_sym_decrypt`); C# aggiornato per usarla. |
| 6 | **Date viaggio — stato "effettuato"** | La colonna **EFFETT.** dell'elenco date mostrava un pallino **verde/rosso senza tooltip**: nessuna spiegazione di cosa indicasse, e il **rosso su "non effettuato"** faceva sembrare un errore quella che per una partenza futura è la situazione **normale**. Il flag inoltre veniva letto **da solo**, mentre il suo significato dipende anche dal calendario: una partenza conclusa e non spuntata è un dato da correggere, una futura e non spuntata è corretta. | Nuovo componente condiviso **`StatoPartenzaChip`** (regole e testi in `StatoPartenzaRules`) che **incrocia il flag con la data di fine** e distingue quattro casi: conclusa e registrata, **conclusa ma non registrata** (anomalia), in programma (normale), **spuntata ma non ancora conclusa** (anomalia). Tooltip sempre presente, che spiega il caso e **dove si corregge**. Adottato sia nell'elenco date sia nelle schede web, così la stessa icona significa la stessa cosa ovunque. |
| 7 | **Partecipanti — titolo del dialogo** | Aprendo i **partecipanti** di una partenza, l'intestazione restava **"Caricamento..."** anche a elenco completamente caricato. Il dato era corretto (`fn_get_viaggio_partecipanti_init_data` restituiva il titolo giusto): il titolo era disegnato con `<TitleContent>`, che il contenitore del dialogo rende **una volta sola** e non rilegge quando il componente termina il caricamento asincrono. | Titolo impostato con **`IMudDialogInstance.SetTitleAsync`**, l'API prevista per i titoli che dipendono da un valore interno al dialogo. ⚠️ Ha effetto **solo** se `<TitleContent>` viene rimosso, quindi il blocco è stato eliminato e non affiancato. Verificati gli altri dialoghi con titolo dinamico: non hanno il problema (titolo da parametro o proprietà calcolata, disponibili già al primo render). |
| 8 | **Date viaggio — cancellazione di una partenza** | Una partenza **già avvenuta** era **cancellabile senza alcuna domanda**: non esisteva nessun controllo né sulle date né sul flag "effettuato". *(Verificato: partenza di 400 giorni fa, segnata come effettuata, eliminata senza avvisi.)* L'unica protezione era **accidentale** — di norma quelle partenze hanno prenotazioni, e quelle bloccavano — quindi non valeva quando i partecipanti non erano mai stati registrati o erano stati rimossi: spariva storico aziendale in modo irreversibile. In più, una partenza con **scheda web** era sì bloccata, ma dal vincolo del database anziché da un controllo: l'utente vedeva un messaggio generico ("utilizzato in altre parti del sistema") in **rosso**, mentre il caso prenotazioni — identico come natura — dava un avviso **giallo**. | Guardie esplicite in `sp_ana_date_viaggi_delete`: si elimina **solo una partenza che deve ancora iniziare** e non segnata come effettuata (stesso confine già usato per la pubblicabilità sul web). Le partenze con una scheda di contenuti web vengono rifiutate con un messaggio che **distingue bozza da pubblicato** e dice cosa fare. Tutti i rifiuti tornano come **avviso giallo**, non come errore. ⚠️ Chi inserisce per sbaglio una data nel passato la corregge e poi la elimina: è deliberatamente un passaggio in due mosse. |
| 9 | **Date viaggio — anno digitato errato** | Una partenza è stata salvata con anno **262** invece di 2026, **senza alcun avviso**. Nessun controllo guardava l'anno: quelli esistenti sono tutti **relativi** (fine ≥ inizio, durata = numero giorni dell'anagrafica) e un refuso sull'anno sposta **entrambe** le date insieme, quindi restano tutti soddisfatti. Riproducendo la maschera fuori dall'applicazione si è visto che il blocco anno **accetta uno zero iniziale** (`0262` è un anno di 4 cifre formalmente valido) e che basta **una sola battuta fuori posto** per produrlo. Il dato sbagliato poi si propaga: la partenza risulta conclusa da secoli, non è pubblicabile e — con le nuove guardie — nemmeno eliminabile. | Controlli **centralizzati** in `DateValidator` con messaggi in `ValidationMessages`: *(a)* **blocco** sugli anni fuori da **2000–2100** (pavimento di plausibilità, non regola commerciale), *(b)* **richiesta di conferma** — non divieto — sulle date insolite ma legittime: anno **precedente a quello in corso** (una partenza non si programma nell'anno scorso) e oltre **5 anni** nel futuro. La conferma è ciò che intercetta il refuso realistico (2027 al posto di 2026), che nessun intervallo può cogliere. Stessa soglia replicata sul database (`chk_data_viaggio_anno_plausibile`), perché il controllo valga anche fuori dalla form. I controlli sono stati estesi anche alla **contabilità** (transazioni e pagamento immediato) con una tolleranza propria: l'anno precedente è lavoro ordinario e non chiede conferma, si conferma da due anni indietro. ⚠️ **Su PROD era già successo**: la transazione **72** aveva data pagamento **20/02/2202** su un movimento del **20/02/2022** — stesso identico refuso, un'altra form, un altro utente. Dato corretto il 2026-08-01. |
| 10 | **Date viaggio — formato data legato alla lingua del PC** *(latente, trovato indagando il n. 9)* | I due campi data del dialogo partenze non avevano **`DateFormat="dd/MM/yyyy"`**, richiesto dalla convenzione interna (`Documents/Digitazione_Date.md`) e già presente in `ClienteDialog`. Senza, il campo non usa `dd/MM/yyyy`: usa il formato breve della **lingua del sistema operativo** — e l'applicazione **non imposta** mai la cultura, quindi eredita quella del PC. Su una macchina configurata in inglese lo short pattern è `M/d/yyyy`: la maschera scrive `01/12/2026` (1° dicembre) e il campo lo rilegge come **12 gennaio**. **Giorno e mese scambiati, in silenzio, senza alcun errore.** Non si è mai manifestato perché la macchina di sviluppo è configurata in italiano — è esattamente il tipo di guasto che compare solo dal cliente. | Aggiunti `DateFormat="dd/MM/yyyy"` e `Placeholder="gg/mm/aaaa"`. Il difetto **non era isolato**: la verifica a tappeto ha trovato **16 campi data** senza `DateFormat` in tutta l'applicazione (transazioni, pagamento immediato, elenco movimenti e i quattro dialoghi di stampa). Sono stati allineati tutti. ⚠️ **Da riverificare sulla partizione Windows 11 prima della consegna**, possibilmente anche con la lingua di sistema impostata su inglese. |
| 11 | **Clienti e Aziende — date senza controlli assoluti** | Estendendo la verifica alle altre anagrafiche è emerso lo stesso schema del n. 9. **Aziende**: *Data Costituzione* non era validata **affatto**, e *Inizio Attività* e *Iscrizione REA* avevano **solo** il confronto con la costituzione — un refuso sull'anno le sposta tutte insieme, quindi i confronti restano soddisfatti e il dato entra. Il messaggio `DataCostituzioneFutura` era definito nei messaggi centralizzati ma **non usato da nessuna parte**: il controllo era stato previsto e mai realizzato. **Clienti**: la *data di rilascio* del documento aveva solo "non futura", quindi un anno assurdo nel passato passava ogni volta che la data di nascita era vuota (l'unico controllo che lo fermava era il confronto con quella); la *data di scadenza* aveva solo "non già scaduto", quindi una scadenza nel **2202** era accettata. Infine la *data di nascita* aveva un limite **relativo** di 90 anni che **rifiutava un cliente di 91**: un dato vero respinto come errore. | Nuovo `CheckDataStorica` in `DateValidator` (anno entro **1900–2100** — il pavimento 2000 delle date operative qui sarebbe sbagliato, chi è nato nel 1960 esiste — più "non futura", con eccezione per le scadenze). Applicato alle tre date azienda e alle tre cliente; il limite dei 90 anni sostituito dal pavimento assoluto, che non respinge più nessun centenario ma ferma comunque i refusi. Conferma oltre **100 anni** indietro. Aggiunti `MinDate`/`MaxDate` a tutti i campi. ✔ Verificato su PROD: **nessun dato errato** in clienti e aziende. |
| 12 | **Anagrafica Cliente — prefisso telefonico mai obbligatorio** | Il prefisso internazionale si poteva lasciare vuoto anche con il numero valorizzato. La radice è la stessa del n. 1, ma al contrario: il validatore **esisteva già** — `ClienteValidator.ValidatePrefissoTelefono`, che restituisce *"Seleziona il prefisso internazionale"* sul valore vuoto — e **non era agganciato al campo**: il `MudTextField` del prefisso era l'unico dei contatti senza attributo `Validation`. Il numero restava così inutilizzabile per un destinatario estero, e la cosa è emersa proprio dalla newsletter, dove l'elenco destinatari mostra il telefono monco. | Validazione collegata al campo, ma **condizionata**: il prefisso è obbligatorio **solo se c'è un numero di telefono**. Renderlo obbligatorio sempre avrebbe bloccato il salvataggio di ogni cliente senza telefono — un dato legittimo — trasformando una svista in un impedimento quotidiano. |

| 13 | **Stampe — Rooming List senza abbinamenti** | Una rooming list **priva di qualsiasi abbinamento cliente/camera veniva stampata ugualmente**, producendo un elenco di nominativi senza camere: un documento che *sembra* una rooming list e non lo è, e che in viaggio si porta dietro l'errore. Il dato per accorgersene c'era già — un partecipante non abbinato arriva dal database con `room_id` nullo e descrizione *«NESSUNA CAMERA ASSEGNATA»* — ma **non lo guardava nessuno**. Non era neppure previsto il caso intermedio: con alcuni clienti abbinati e altri no si stampava in silenzio, e i mancanti risultavano invisibili sul prospetto. *(Casi riproducibili sui dati reali: partenze **65** e **66** con 34 partecipanti e **zero** abbinamenti; partenza **262** con 46 partecipanti di cui **1** non abbinato.)* | Tre regole, tutte in **un unico punto**: **(a)** nessun partecipante → non si stampa; **(b)** **nessun abbinamento** → non si stampa e si dice perché, indirizzando alla gestione partecipanti; **(c)** **abbinamenti parziali** → si chiede conferma (*«Ci sono N clienti non abbinati a una camera su M: vuoi proseguire lo stesso?»*) e, se si prosegue, il **PDF riporta l'avviso in evidenza sulla prima pagina** — chi legge il prospetto stampato non ha visto il messaggio a video. ⚠️ Il flusso di stampa era **duplicato in cinque schermate** (menu, gestione partecipanti, elenco date, dashboard admin e superadmin): aggiungere i controlli in cinque copie avrebbe significato cinque occasioni di dimenticarne una, e la sesta schermata sarebbe nata senza. È stato quindi accentrato in `RoomingListStampaService`, che ora è l'unico punto da cui si stampa una rooming list; le cinque copie (43+25+25+43+43 righe) sono diventate una chiamata. Nel farlo è emerso che il menu aveva anche una **propria** procedura di apertura del PDF, diversa da quella condivisa: ora tutte le schermate si comportano allo stesso modo. |
| 14 | **Selettore viaggio/partenza — scheda «Per Anno»** | Scegliendo una partenza dall'albero della scheda **«Per Anno»**, la partenza **non risultava selezionata**: si poteva evidenziarla senza che la scelta venisse presa. La scheda «Ricerca Rapida» (le due tendine) funzionava, quindi il difetto restava nascosto a chi usava quella. È un difetto **anteriore all'estensione web** — il selettore nasce con `f1c4399` — e riguarda **sei schermate**, perché il componente è condiviso: menu stampe, dashboard admin e superadmin, blocco tour della newsletter, riaggancio della partenza e filtri sui destinatari. *(Verificato che non fosse un problema di dati: le due schede leggono da function diverse — `get_viaggi_grouped_by_year` e `get_datetrips_fromtrip` — e sui dati reali restituiscono **esattamente gli stessi 20 record**, zero differenze in entrambi i versi. Verificate anche le impostazioni del componente: `SelectionMode` è `SingleSelection` e `ReadOnly` è `false`, quindi la selezione era permessa.)* | Le foglie dell'albero — le partenze — dichiaravano `CanExpand` al valore predefinito **`true`**, che la documentazione di MudBlazor descrive come pensato per gli elementi **caricati pigramente**: su un elemento senza figli fa interpretare il clic come un tentativo di espansione invece che come una scelta. Ora le foglie hanno `CanExpand="false"`, che è anche la descrizione corretta di ciò che sono. In più la scelta è stata resa **indipendente da quale delle due strade il TreeView percorra**: oltre alla selezione, la foglia gestisce il clic diretto, e le due strade chiamano lo **stesso** metodo `SelezionaPartenzaAsync`, idempotente. Nel farlo è emerso che il metodo non ricaricava le date del viaggio quando `_dates` era vuoto pur essendo il viaggio già quello giusto: la partenza risultava scelta ma il riepilogo restava vuoto. |
| 15 | **Anagrafica Azienda — logo, scheda «SEO e Brand»** | Il campo *Aggiungi parola chiave* dichiara **«Premi INVIO o clicca + per aggiungere»**, ma **INVIO non aggiungeva niente**: nessun messaggio, nessuna parola in elenco. Il `+` funzionava. Il gestore del tasto c'era ed era corretto (`e.Key == "Enter"` → `AddSeoKeyword`), e anche il metodo lo era: usciva solo perché `_newSeoKeyword` risultava **vuota**. La causa è che `@bind-Value` di `MudTextField` aggiorna la variabile **all'uscita dal campo**, non a ogni tasto: premendo INVIO senza spostare il fuoco, il gestore leggeva il valore precedente. Il `+` funzionava per un caso fortunato — cliccarlo toglie il fuoco al campo, quindi il valore veniva scritto un istante prima che partisse il click. *(Difetto preesistente: la label nasce con il commit `5424ea7`.)* | Aggiunto `Immediate="true"` al campo, così la variabile segue la digitazione e INVIO trova ciò che l'utente ha appena scritto. ⚠️ **Verificato lo stesso schema altrove**: in `RepartoAziendaleDialog` un campo con `OnKeyDown` che invia la form ha la stessa struttura, ma è `Required` con validazione, e `Immediate` cambierebbe anche *quando* compare il messaggio di errore. Non è stato modificato alla cieca: va provato digitando un nome e premendo INVIO **senza** uscire dal campo. |
| 16 | **Dialoghi — focus sul primo campo all'apertura** | Aprendo *Nuovo Reparto Aziendale* il cursore **non si posiziona sul primo campo**, che è un requisito fisso di queste form. Il codice però c'era: il dialogo chiama `dialogFormHelper.setupTabNavigation`, e quell'helper **fa** il focus su `fields[0]`. Il punto è *quando*: lo fa a **+150 ms**, e MudBlazor prende il fuoco per sé mentre apre il dialogo, **dopo** di lui. I dialoghi in cui il focus funziona compensano ribadendolo da C# a **+300 ms** — è uno schema presente in **28** file, e chi non ce l'ha resta senza focus. Non era quindi una dimenticanza isolata ma una **corsa persa**, mascherata dal fatto che la maggioranza dei dialoghi aveva la contromisura. | Due interventi. **(a)** Sul dialogo segnalato, lo stesso schema dei 28 che funzionano: `FocusAsync` sul campo dopo 300 ms. **(b)** L'helper JS ora **insiste**: riprova il focus a intervalli di 150 ms, fino a cinque volte, e si ferma appena il fuoco è dentro al dialogo. La condizione «è già dentro» è la parte che conta: impedisce di calpestare i dialoghi che scelgono da sé quale campo attivare. ⚠️ **Ricognizione**: su 52 punti che chiamano l'helper, **28** ribadiscono il focus e **24** no — ma fra questi ultimi ci sono le schede dei contenuti web e i dialoghi di stampa, dove forzare il focus sarebbe sbagliato. L'intervento **(b)** li copre tutti senza toccarli uno per uno; quali fossero davvero da correggere andrà confermato provandoli. |
| 17 | **Newsletter — anteprima: l'applicazione si chiudeva da sola** | Aprendo l'anteprima di una newsletter e **chiudendola prima che avesse finito di caricare le immagini**, dopo qualche secondo si chiudeva l'**intera applicazione**. Nessun messaggio, nessun errore a video. La sequenza minima: apri l'anteprima → cambia lingua → chiudi → torna all'elenco → attendi. La causa è nella scrittura dell'anteprima: `newsletterPreview.scrivi` non restituisce subito, **aspetta che le immagini siano caricate** e si arrende solo dopo **10 secondi** — è ciò che tiene su il velo di attesa. Se nel frattempo il dialogo viene chiuso, quella chiamata torna verso un componente ormai distrutto, e in MAUI un'eccezione che nasce lì non la raccoglie nessuno: il processo termina. Il ritardo di «qualche secondo» prima del crollo è esattamente quel timeout. ⚠️ **Difetto preesistente e latente**: la stessa chiamata senza protezioni c'era già alla prima apertura dell'anteprima; l'aggiunta del cambio lingua l'ha reso facile da incontrare, perché ogni cambio ne avvia un'altra e si chiude naturalmente subito dopo aver guardato. | Il dialogo ora possiede un **gettone di annullamento**: chiudendolo la chiamata viene staccata invece di restare appesa, e `OperationCanceledException` e `JSDisconnectedException` vengono riconosciute come normali chiusure, non come errori. In più il componente segna quando viene smontato e **dopo ogni attesa** verifica di esistere ancora prima di toccare lo stato — la stessa protezione applicata anche alla ricomposizione per lingua, che dura anch'essa il suo tempo. Verificato che nessun altro punto dell'applicazione usi quella scrittura, e che l'unica altra chiamata JS in attesa (`getWindowWidth`) ritorni immediatamente. |
| 18 | **Newsletter — la composizione si chiudeva da sola dopo l'anteprima** | Chiusa l'anteprima si tornava correttamente alla newsletter, ma dopo qualche secondo la form si chiudeva **da sola** e si tornava all'elenco. Nessun messaggio. Diagnosticato solo dopo aver aggiunto il registro su file, dove e' comparso: *«BlazorWebViewHandler — There was an error trying to open URL: about:blank»*. L'anteprima scrive il suo HTML dentro un `<iframe>` **senza indirizzo**; la WebView legge quella scrittura come una **navigazione** verso `about:blank`, e il comportamento predefinito di `BlazorWebView` davanti a un indirizzo che non riconosce e' **aprirlo nel browser di sistema**. L'apertura fallisce, e nel fallire l'albero Blazor viene ricostruito da capo: la pagina riparte dallo stato iniziale, cioe' dall'elenco delle newsletter. ⚠️ Due tentativi precedenti di spiegare il difetto **leggendo il codice sono stati sbagliati**: la causa non era in nessun percorso dell'applicazione, ma nell'ospite che la contiene. | Gestito `BlazorWebView.UrlLoading`: gli indirizzi `about:` e `blob:` restano **dentro** la WebView invece di essere trattati come collegamenti da aprire altrove. Sono navigazioni interne alla pagina, non richieste dell'utente. |
| 19 | **Anagrafica Cliente — il titolo non si vedeva, e poteva contraddire il sesso** | Due difetti nello stesso campo. **(a)** La tendina *Titolo* era cablata nel dialog con voci `Sig.`, `Sig.ra`, `Dott.`…, ma `ClienteService.NormalizeCliente` forzava il **maiuscolo** al salvataggio: `Sig.` diventava `SIG.`, che alla riapertura non corrispondeva più a nessuna voce e **il campo appariva vuoto**. Ai 728 clienti importati da Oracle, che avevano i codici `SIG`/`SRA` mai presenti in tendina, il titolo non è **mai** comparso. Essendo il campo `Required`, chi salvava era costretto a sceglierne uno nuovo, riscrivendo il dato in un formato ancora diverso. **(b)** Il sesso era un campo a sé: nulla impediva di salvare un titolo maschile su una donna, e su PROD era successo a **11 clienti** (`SIG` con sesso `F`). Non è distrazione degli utenti, è una possibilità che lo schema concedeva. | Il titolo diventa una **lookup** (`ana_titolo_persone`, codice + descrizione + sesso) con FK numerica: il maiuscolo non può più rompere il collegamento. Il **sesso si deriva dal titolo** e in anagrafica è in sola lettura. L'invariante vive in un **trigger sul database**, non nell'interfaccia, perché `ana_clienti` si scrive anche dal sito di iscrizione, dagli `sp_ana_clienti_*` e dagli import: una regola che sta solo nella form non è una regola. Dove titolo e sesso si contraddicevano ha vinto il **sesso** (`SIG` è il default dell'import Oracle, il sesso è stato messo a mano). Aggiunta la pagina di gestione dei titoli sotto il nuovo gruppo di menu *Tabelle Anagrafiche*. ⚠️ Un ponte di compatibilità mantiene funzionante il **sito di iscrizione** senza modificarlo: verificato su inserimento, rilettura e aggiornamento, anche con titolo e sesso in contraddizione fra loro. |
| 20 | **Anagrafica Cliente — l'unicità di email e codice fiscale poteva essere approvata al buio** | Il controllo che impedisce due clienti con la stessa email nella stessa azienda riceve l'azienda dal dialogo, e i chiamanti gliela passano con un fallback: `_currentUser?.AziendaId ?? 0`. **Lo zero non è un'azienda, è «non lo so»** — ma veniva trattato come un'azienda vera: la ricerca girava su un'azienda inesistente, non trovava nulla e **approvava**. Stesso schema per il controllo sul codice fiscale. | Un unico punto, `AziendaDaControllare()`, che restituisce `null` quando l'azienda non si sa. Per un utente normale l'azienda ignota non fa più passare in silenzio: dice che non ha potuto verificare. Per il SuperAdmin che non ha ancora scelto l'azienda resta silenzioso, perché lì il campo *Azienda* è già obbligatorio e blocca comunque il salvataggio. ⚠️ **Il difetto non poteva sporcare i dati**: l'azienda zero non esiste, quindi il salvataggio sarebbe fallito comunque sulla chiave esterna. L'effetto reale era far arrivare un errore tecnico incomprensibile al posto di *«email già presente»*. |
| 21 | **Anagrafica Cliente — le regole vivevano solo nel C#, dove il sito non arriva** | Il database accettava quasi tutto: su `ana_clienti` esisteva **un solo vincolo** (`cliente_sesso IN ('M','F')`), mentre 17 metodi di `ClienteValidator` più le annotazioni del model imponevano le regole **soltanto nella form desktop**. Dal sito di iscrizione poteva quindi entrare un'anagrafica che il gestionale avrebbe rifiutato — email malformata, IBAN inventato, documento che scade prima di essere stato rilasciato — e non se ne accorgeva nessuno finché quel cliente non veniva riaperto a video. | Gli **invarianti veri** sono scesi nel database come `CHECK` (`SqlScripts/541`): formato email, minimi di due caratteri su nome e cognome, documento non rilasciato prima della nascita né scaduto prima del rilascio, forma dell'IBAN. Misurati sui dati reali di PROD, **zero righe li violano**: nessuna bonifica, e valgono per gestionale e sito dal primo istante. Messaggi tradotti in italiano in `DatabaseExceptionHelper`. ⚠️ *«Data di rilascio non nel futuro»* **non** è diventata un vincolo: un `CHECK` dev'essere `IMMUTABLE` e `CURRENT_DATE` non lo è. Stessa ragione, oltre che concettuale, per *«documento scaduto»*, che resta un avviso — un documento scade da solo col tempo, e un vincolo renderebbe la scheda non più salvabile l'indomani. |
| 22 | **Newsletter — la tabulazione non tabulava e nessun campo prendeva il fuoco** | Nella pagina Newsletter il TAB non spostava fra i campi e all'apertura il cursore non si posava da nessuna parte. La pagina non agganciava `dialogFormHelper` — non iniettava nemmeno `IJSRuntime` — mentre lo fanno 21 altri componenti, dialoghi e pagine a filtri comprese. | Aggancio unico sul contenitore della pagina, **riagganciato a ogni render**: `MudTabs` rende il solo pannello attivo e la vista di composizione si completa in asincrono, quindi un aggancio unico incatenava i campi di un DOM ancora a metà. Il fuoco invece **non** lo dà l'helper ma il codice C# con `@ref`/`FocusAsync`: il «primo campo» dell'helper è il primo del DOM, che lì sarebbe una casella di spunta. |

| 23 | **Anagrafica Cliente — dati che il database accettava e non avrebbe dovuto** | Oltre al punto 21, altre tre regole vivevano solo nel C#. Sui dati reali di PROD: **2 telefoni** con separatori non previsti, **4 codici fiscali** non di 16 caratteri (tre troncati, uno che non è un codice fiscale — tredici cifre), **8 indirizzi** di residenza che sono spazzatura (`32`, `A`, `VIA`, `QQQ`, e uno **stringa vuota**). | Tre `CHECK` (`SqlScripts/542`). Sul **telefono** il caso si è rivelato diverso: le due violazioni erano numeri veri scritti col punto e con la barra — separatori italiani comunissimi — quindi era la **regola** a essere troppo stretta, non il dato: allargata a `.` e `/`, e il vincolo nasce valido su tutto. **Codice fiscale** e **indirizzo** sono difetti veri e non si possono indovinare: i vincoli sono `NOT VALID`, che su insert e update è identico a un vincolo normale ma non torna indietro a bocciare lo storico. ⚠️ Riaprendo e **salvando** una di quelle 12 schede il vincolo scatta e il dato va sistemato prima di poter salvare: è una bonifica graduale, non un blocco permanente. Dieci delle dodici sono dell'azienda 6. |

| 24 | **Codice fiscale — 16 anagrafiche incoerenti, mai rilevate da nessuno** | Portato il motore del codice fiscale nel database (era duplicato in C# e in Python, con la copia C# orfana), lo si è fatto passare sulle **448 anagrafiche reali** che un codice fiscale ce l'hanno. Ne sono emerse **16 incoerenti**, presenti da sempre e mai segnalate perché nessun controllo confrontava il codice con l'anagrafica: **3 con nome e cognome invertiti** (fra cui `NATRELLA`, lo stesso su cui l'avviso nome/sesso del punto 19 aveva sollevato il sospetto), **3 con la data di nascita sbagliata**, **5 con il comune di nascita sbagliato**, **3 con il codice di un'altra persona** (il cognome torna, il nome no — verosimilmente un familiare) e **3 con il codice malformato**, tutti di clienti nati all'estero. | Bonificate le **11 riparabili**, su PROD e in locale, con uno script idempotente in cui **ogni regola si applica solo se rende il codice corretto** — la garanzia di non «correggere» una riga peggiorandola. Su PROD le corrispondenze passano da 413 a **424** e le incoerenze da 16 a 6. Le 6 restanti non sono indovinabili: servono le persone. ⚠️ Cercando i comuni italiani privi di codice catastale se ne è trovato **uno solo, BOMBAY** — che italiano non è: puntava già alla provincia «ESTERO - INDIA» ma aveva il flag sbagliato. Corretto con una regola generale. Tutti gli 8092 comuni italiani hanno il loro codice. |

| 25 | **Iscrizione al viaggio — dall'Iscrizione Veloce un pilota non si poteva iscrivere** | I campi del mezzo — **marca, modello, targa** — esistevano solo nel tab Partecipanti. Nell'**Iscrizione Veloce** (`QuickAddParticipantDialog`) non c'erano affatto: si sceglieva un ruolo da pilota, il controllo li pretendeva (`tipo_partecipante_dati_mezzo_obb`, verificato da `fn_mov_clienti_viaggi_insert`) e la form non dava modo di compilarli. Un rifiuto **senza via d'uscita**: da lì un pilota non era iscrivibile in nessun modo. Il difetto è emerso solo ora perché fino al 2026-08-21 quella form chiamava `sp_mov_clienti_viaggi_create`, che non controllava nulla — l'iscrizione passava, e il mezzo restava semplicemente vuoto. | Il blocco è uscito da `ViaggioPartecipantiManagerDialog` ed è diventato il componente condiviso **`DatiMezzoPartecipante`**, usato da **entrambe** le form. Copiarlo nella seconda sarebbe stato più rapido, ma avrebbe rimesso in piedi la stessa regola in due posti. Il componente non decide se mostrarsi: lo decide la form, leggendo `tipo_partecipante_pilota` **dal dato** e mai dalla descrizione del ruolo (guidano anche «GUIDA IN SECONDA» e «PILOTA MOTO PROPRIA»). |
| 26 | **Anagrafica Cliente — gli errori del database non dicevano quale campo** | Dopo la centralizzazione dei controlli (`SqlScripts/541`–`552`) le regole stavano nel database, ma le sue risposte arrivavano **solo come messaggio a video**: il campo respinto restava indistinguibile dagli altri. Peggio, si scoprivano **solo premendo Salva** e **solo se nient'altro era in errore**, perché la validazione della form usciva prima con un generico *«ci sono errori di validazione»*. Chi salvava da una scheda diversa da quella del campo sbagliato non aveva modo di sapere dove tornare. | Ogni esito del database ha ora un campo (mappa `CampoPerEsito` in `ClienteDialog`) e lo **illumina**; i controlli si fanno **uscendo dal campo**, non solo al salvataggio; il messaggio generico è sostituito dagli errori veri. Nessuna regola è stata copiata in C#: restano nel database, e ogni regola futura si aggancia aggiungendo una riga alla mappa. |
| 27 | **Anagrafica Cliente — errori che restavano accesi su campi già corretti** | Le tre date (nascita, rilascio, scadenza) **si giudicano a vicenda**, ma MudBlazor rivalida solo il campo che cambia: correggendo la data di rilascio l'errore restava, perché la causa stava nella data di nascita e nessuno la ricalcolava. Il messaggio per giunta accusava il campo sbagliato. | Al cambio di una data si rivalidano anche le altre — **solo quelle già toccate**, altrimenti comparirebbe «obbligatoria» su una data mai aperta. |
| 28 | **Anagrafica Cliente — il CAP spariva e il TAB si fermava su campi non digitabili** | I riquadri CAP/Regione/Prov./Naz. erano `MudTextField`, cioè **input veri**: entravano nel giro del TAB (il fuoco ci finiva e l'etichetta si spostava) e, soprattutto, MudBlazor **scarta l'aggiornamento del testo mentre il campo ha il fuoco** (`TextUpdateSuppression`, attiva perché in MAUI Hybrid la libreria si crede lato server). Scegliendo il comune e premendo TAB, il CAP arrivava **dopo** che il fuoco era già lì: restava vuoto. In modifica non si vedeva, perché lì il valore c'era già al primo render. | Sostituiti con `MudField`, che non è un input: fuori dal giro del TAB e sempre aggiornato. Stessa ragione per il campo **Sesso**, che lo scrive il titolo. |
| 29 | **Dashboard — il filtro per anno non cambiava i dati** | Scegliendo un anno diverso le statistiche restavano su quello corrente. La lista aveva **due canali per la stessa scelta**: `@bind-SelectedValue="_selectedYear"` *e* `OnClick="() => OnYearSelected(year)"` su ogni voce. Il binding scriveva `_selectedYear` per primo, e la guardia `if (_selectedYear != year)` trovava l'anno **già scritto**: le statistiche non venivano mai ricaricate. | Un canale solo (`SelectedValueChanged`), e la guardia torna a fare il suo mestiere. |

| 30 | **Dialoghi di conferma — un pulsante rosso «Elimina» per operazioni che non cancellano nulla** | `DeleteConfirmationDialog` è il dialogo condiviso usato in **31 punti** dell'applicazione. Testo del pulsante e colore erano già parametrici, ma l'**icona era cablata sul cestino**: chi lo riusava per una conferma qualsiasi si ritrovava a proporre un pulsante **rosso con l'icona del cestino** per un'operazione innocua. Visto durante i test del codice fiscale: alla domanda «nome e cognome sembrano invertiti, confermi?» si rispondeva premendo **«Elimina»**. | Icona resa parametrica (`Icon`, default invariato sul cestino, così i 31 usi esistenti non cambiano). Le conferme non distruttive passano testo, colore e icona neutri. |

| 31 | **Anagrafica Cliente — i messaggi sui duplicati rivelavano il nome di clienti altrui** | Digitando un codice fiscale o un'email già presenti, la segnalazione rispondeva con **nome e cognome** dell'altro cliente che li possiede: un dato che chi inserisce non aveva digitato, non gli serve per correggere il proprio inserimento, e che bastava provare codici a caso per farsi dire. | **Nessuna segnalazione sui duplicati nomina più nessuno** (`SqlScripts/566` per codice fiscale ed email, `567` per stessi dati anagrafici e omonimo). Il 566 aveva lasciato il nome dove era già sullo schermo perché l'utente l'aveva appena digitato; il 567 ha reso la regola netta e più semplice da ricordare: **mai un nome**. Il messaggio dice cosa è successo, e sul codice fiscale anche **come trovarla** quella scheda — cercandola per codice fiscale. |
| 32 | **Anagrafica Cliente — eccezione aprendo un cliente con più segnalazioni sullo stesso campo** | *(Difetto introdotto e chiuso il 2026-09-01, mai rilasciato.)* Il controllo che riporta gli esiti del database sui campi li indicizzava per campo di destinazione con `ToDictionary`. Quando due esiti diversi ricadevano **sullo stesso campo** — sul codice fiscale «già registrato» e «non corrisponde» arrivano insieme — la chiave si duplicava e il dialogo cadeva in `ArgumentException`. | Gli esiti si **raggruppano** invece di indicizzarsi, e il campo mostra il primo, che è il più grave nell'ordine in cui il database li emette. |

| 33 | **Consenso newsletter — si poteva dare senza un indirizzo a cui scrivere, e senza dire da dove veniva** | Due difetti sulla stessa colonna, trovati insieme nel test F1. *(a)* Si spuntava il consenso **lasciando l'email vuota**: le tre colonne venivano scritte regolarmente — quindi la prova c'era — ma non c'era nessuno a cui mandare niente. *(b)* La **fonte** del consenso, che serve a dimostrare *dove* è stato raccolto, aveva smesso di essere valorizzata: da quando il consenso viaggia dentro il salvataggio del cliente, il dialogo la impostava **solo se ce n'era già una**, quindi ogni consenso nuovo nasceva `NON_DICHIARATA`. Nei dati si vede il prima e il dopo: sette clienti con fonte `gestionale`, poi il vuoto. | *(a)* Regola nel database (`SqlScripts/568`), così varrà anche per il sito, dove il consenso è ancora da aggiungere; nella form la spunta non si accende finché manca l'email e lo dichiara — ma resta sempre **spegnibile** anche senza, perché una revoca non si nega mai. *(b)* Un consenso dato dal gestionale registra ora `GESTIONALE`; uno che c'era già **conserva la sua origine**, che è quella vera e non va riscritta a ogni salvataggio. |

| 34 | **Consenso newsletter — la scheda chiamava «data di revoca» una data di concessione** | Revocando il consenso, la scheda mostrava «**Revocato il** *data*». Ma quella data il database la scrive **solo** nel passaggio da spento ad acceso e la lascia intatta alla revoca — proprio perché serve a dimostrare che il consenso un tempo c'era. L'etichetta la presentava quindi come data di revoca, che non è registrata da nessuna parte: chi avesse dovuto ricostruire una contestazione avrebbe letto una data sbagliata su una colonna che esiste apposta per fare da prova. | La scheda dice ora «**Revocato. Era stato concesso il …**». Nessuna modifica ai dati: cambiava solo cosa se ne capiva. |

| 35 | 🔴 **Campi data — la maschera perdeva e riordinava le cifre, e ha scritto dati sbagliati in produzione** | Digitando in fretta, `DateMask` produceva date diverse da quelle battute. **Non è teoria: il 2026-08-31 una partenza reale è stata registrata su PROD come `8202-01-19` invece di `2026-08-19`, con dodici iscritti attaccati** — scritta dalla 1.35 *mentre* collaudavamo. Le cifre non erano casuali: `19082026` → `19018202`, slittate di una posizione con l'ultima caduta fuori, cioè un carattere inserito dove il modello della maschera non era ancora arrivato. **Il meccanismo**: con `Mask`, ogni tasto viene intercettato in JavaScript e *impedito* al browser, rispedito a .NET, il testo ricalcolato e riscritto, e infine il **cursore riposizionato con un secondo viaggio verso JavaScript**. Su MacCatalyst ogni messaggio .NET→JS è una `evaluateJavaScript` separata (4-8 per tasto, già misurate nella diagnosi dei crash WebKit): se il tasto dopo arriva prima della fine del ciclo, entra col cursore fermo dov'era. **Perché era rimasto nascosto**: un commento nel codice affermava che «in MAUI Hybrid il rendering è client-side, quindi l'esperienza è fluida; il bug MudBlazor #6796 affligge Blazor Server». È l'opposto — `RuntimeLocation.IsServerSide` guarda `OSDescription != "Browser"`, quindi in MAUI Hybrid MudBlazor si crede lato server: eravamo nel caso peggiore mentre il commento ci diceva di stare nel migliore. | `Mask` **rimossa da tutti e 32 i campi data** dell'applicazione (14 file: anagrafiche, contabilità, viaggi, aziende, stampe) e sostituita da `Helpers/ConvertitoreDataFlessibile`. La comodità di battere solo le cifre non stava nella maschera ma nel **convertitore**, che legge il testo una volta sola quando è finito: si continua a digitare `15031990`, e sono accettati anche `15/03/1990`, `15.03.1990`, `15-3-90`. Nessuna intercettazione per tasto, nessun riposizionamento del cursore. Convenzione aggiornata in `Documents/Digitazione_Date.md`. ⚠️ **Sulla partenza corrotta serve una correzione dati su PROD**: vedi §2.9 della Checklist Go-Live. |

| 36 | **Elenchi — non si trovava ciò che avevano appena scritto gli altri** | La casella delle griglie **non interroga il database: filtra la lista caricata all'apertura della pagina**. Un cliente creato nel frattempo dal sito di iscrizione, o un viaggio inserito da un collega, in quella lista non c'è — quindi nessuna ricerca può trovarlo, e bisognava uscire dalla pagina e rientrare (è il rientro a rileggere). Non esisteva nemmeno un modo di *ripetere* la ricerca, ed era coerente: ripeterla avrebbe filtrato di nuovo la stessa fotografia. Finché il gestionale era l'unico a scrivere il presupposto reggeva; **è saltato quando il sito ha cominciato a scrivere nella stessa anagrafica, e quando in ufficio si è iniziato a lavorare in più persone**. | Tre elenchi cercano ora nel database: **clienti** (`fn_search_clienti`), **viaggi** (`SqlScripts/570`, un parametro in più sulla funzione esistente invece di una seconda copia della SELECT) e **controparti** (`SqlScripts/571`). Le liste rimanenti (movimenti, aziende, utenti) hanno un pulsante di aggiornamento e il tasto Invio: sono sotto le 150 righe e le scrive quasi sempre una persona sola. ⚠️ Prima si era provata la strada del **solo pulsante** anche sui clienti: funzionava, ma è stata giudicata peggiore del difetto — la stessa casella si comportava in due modi, e distinguere i due casi richiede all'utente di sapere *quando* un record è nato, l'unica cosa che non può sapere. |
| 37 | **Elenchi — la casella di ricerca si riprendeva il fuoco mentre si digitava** | `EnterpriseGridToolbar` rimetteva il cursore nella casella **600 ms dopo ogni ridisegno della griglia**, non solo all'apertura. Se il ridisegno arrivava mentre si scriveva, il testo si spezzava in due tronconi. Difetto latente da sempre, diventato evidente provando a far ricaricare la griglia a ogni battuta. | Il fuoco viene messo **solo su una casella vuota**: se c'è già del testo, ce l'ha chi sta scrivendo, e riprenderselo significa spezzargli la parola. |
| 38 | **Elenco viaggi — il testo cercato spariva dal campo dopo aver trovato** | La pagina mostrava la barra di attesa **al posto** della griglia (`@if (_isLoading) { barra } else { griglia }`): ogni ricerca la toglieva dal DOM e la ricreava da zero. Ma il testo cercato la griglia lo tiene al proprio interno, quindi ripartiva vuota — si digitava `pir`, comparivano i Pirenei, e `pir` si cancellava da solo. Nei clienti non accadeva perché lì il caricamento passa dal parametro `Loading` e la griglia resta viva. | La barra sostituisce la pagina **solo al primo caricamento**, quando non c'è ancora niente da perdere; durante le ricerche l'attesa la mostra la griglia stessa. Verificate le altre pagine con griglia: nessun'altra usava quello schema. |
| 39 | 🔴 **Controparti — il confine fra i silos delle aziende era scritto concatenando una stringa** | Trovato portando la ricerca nel database. `ContropartiService.GetAllAsync` costruiva la query **in C#**, e il filtro che impedisce a un'azienda di vedere le controparti di un'altra era questo: `sql += $" AND c.azienda_fk = {currentAziendaId.Value}"`. Oltre a violare il DB-first, significa che **l'invariante dei silos dipendeva da un'interpolazione di stringa**: regge perché quel valore è un intero preso dalla sessione, ma non è una riga che si vuole trovare in un audit, e basta una modifica distratta per renderla pericolosa. | Lettura spostata in `fn_ana_controparti_get_all` (`SqlScripts/571`), dove il filtro azienda è un **parametro** come tutti gli altri. ✅ **Cercato in tutto il gestionale**: i punti erano **due**, e sono stati chiusi entrambi — le controparti e le **aziende** (`SqlScripts/572`), dove nella stessa query il filtro per anno era già un parametro `@year` mentre quello per azienda era concatenato. Chi l'ha scritta sapeva parametrizzare, e proprio sulla riga che separa un'azienda dall'altra ha incollato una stringa: è così che nascono queste cose, non per ignoranza ma perché quel valore «viene da noi» e sembra sicuro. ⚠️ **Resta da decidere**: `TenantContext.GetTenantFilterSqlAsync` e i due metodi di `BaseCrudService` che lo avvolgono **generano frammenti SQL** per il filtro tenant (`$"{columnName} = {aziendaId.Value}"`). Oggi non li chiama nessuno — sono codice morto — ma chi li trovasse penserebbe che siano il modo giusto di filtrare per azienda. |

### Bug individuati durante l'analisi, **non ancora risolti**

Emersi il 2026-08-20 analizzando validazioni e CRUD di `ana_clienti` in vista della centralizzazione
dei controlli. Sono documentati qui perché la loro gravità non dipende da quando verranno chiusi.

| # | Area | Problema | Stato |
|---|------|----------|-------|
| A | **Anagrafica Cliente — omonimi e schede duplicate** | Il controllo anti-duplicato anagrafico **esiste ed è collegato**, ma è racchiuso in `if (DataNascita.HasValue && !string.IsNullOrWhiteSpace(CodiceFiscale))`: **gira solo se ci sono sia la data di nascita sia il codice fiscale**, e la query che esegue pretende per giunta **l'uguaglianza del codice fiscale** fra i criteri di ricerca. È quindi cieco esattamente sulle schede che hanno più probabilità di essere duplicate: quelle incomplete. Su PROD **327 clienti su 778 (42%) non hanno il codice fiscale** — su nessuno di loro quel controllo è mai stato eseguito. Risultato: **5 gruppi di omonimi** già presenti nella stessa azienda. *(Caso reale: due schede di **ANTONIO TOLU**, stessa azienda, stesso comune di nascita, stessa residenza, stesso indirizzo, stesso telefono — e date di nascita che differivano di un mese per un refuso. Fuse a mano su PROD il 2026-08-20: 3 viaggi e 3 righe di alloggio consolidati.)* | **Da correggere.** Proposta a tre livelli, nessuno dei quali deve pretendere il CF per funzionare: stesso **CF** → blocco; stessi cognome, nome, **data e comune di nascita** → blocco; stessi cognome e nome soltanto → **avviso**, perché l'omonimia esiste davvero. |
| B | **Iscrizione al viaggio — nessuna validazione** | `MovClientiViaggiService` non ha **alcun** controllo: solo traduzione in italiano degli errori del database. Si può iscrivere come **pilota** una persona **senza email** e nulla lo impedisce — su PROD è successo **11 volte**. | **RISOLTO** in due tempi. Le regole sono scese nel database il 2026-08-20 (`SqlScripts/551`, `553`): email obbligatoria per chi guida, dati del mezzo quando il ruolo li richiede, doppia iscrizione impedita. Ma fino al **2026-08-21** `MovClientiViaggiService` chiamava ancora `sp_mov_clienti_viaggi_create`: le nuove regole le applicava **solo il sito**, e il gestionale — lo strumento di chi lavora tutti i giorni — restava scoperto. Ora entrambe le form passano da `fn_mov_clienti_viaggi_insert`. In più, quando manca l'email di chi guida non si rifiuta e basta: si **chiede**, con un popup che la salva in anagrafica passando dalla validazione normale. Rifiutare e basta è il modo migliore per far nascere l'abitudine di non iscrivere nessuno — è così che sono entrati gli 11 piloti senza email. |
| C | **Anagrafica Cliente — le regole si contraddicono fra loro** | Email, telefono e i campi del documento sono dichiarati **obbligatori** dall'annotazione del model **e** dal validatore di business, ma il delegato del dialogo — lo strato più nascosto dei tre, e quello che MudBlazor usa davvero — li lascia passare vuoti (`if (IsNullOrWhiteSpace(email)) return [];`), e nel markup quei campi non hanno `Required`. Non esiste oggi una risposta alla domanda *«l'email è obbligatoria?»*. La prova sta nei dati: **297 clienti su 778 non hanno email** e convivono da sempre con un'annotazione che li dichiara impossibili. | **Da decidere prima di correggere.** Deciso il 2026-08-20 che il criterio è il **ruolo**: obbligatoria per i piloti, facoltativa per gli accompagnatori — che è già come funziona il sito di iscrizione. Misurato su PROD: chi guida lascia l'email nel **98,4%** dei casi, chi è trasportato nel 28%. |
| E | **Codice fiscale — il motore di verifica esiste e non lo chiama nessuno** | `Validation/Fiscal/CodiceFiscaleValidator` contiene, **scritto per intero e funzionante**, un motore che *calcola* il codice fiscale atteso da cognome, nome, data di nascita, sesso e comune (`CalculateExpectedCodiceFiscale`), lo *confronta* con quello digitato (`ValidateAgainstAnagrafica`) e gestisce perfino le sostituzioni per *omocodia* (`CheckOmocodia`). **Nessuno dei tre metodi ha chiamanti.** L'unico usato controlla la sola forma della stringa. È lo strumento che avrebbe intercettato tutti i casi emersi il 2026-08-20: i 4 codici fiscali malformati e il doppione di ANTONIO TOLU — il cui codice, `TLONTN77M24G113N`, contiene data (24 agosto 1977), sesso e comune (`G113` = Oristano) e li conferma tutti. | **RISOLTO** (`SqlScripts/544`–`548`, `555`). Il motore è sceso nel database — `fn_cf_calcola`, `fn_cf_verifica`, `fn_cf_decodifica`, `fn_cf_verifica_cliente` — dove stanno i codici catastali e dove lo raggiunge **anche il sito**, che ne aveva una terza copia in Python (`codice_fiscale_utils.py`, eliminato). I tre metodi orfani in C# sono stati rimossi il 2026-08-21: ~265 righe, la seconda copia dell'algoritmo. Di `CodiceFiscaleValidator` resta il solo controllo di forma, che serve a dire subito che quella stringa non può essere un codice fiscale. |
| D | **Validatori scritti e mai collegati** | Quattro metodi di `ClienteValidator` non hanno chiamanti. Due sono superati dal punto 19 (`ValidateTitolo`, `ValidateSesso`). Gli altri due — `ValidatePassengerEmailDifferentFromPilot` e `ValidatePassengerEmailUnique` — codificano l'assunto **opposto** a quello verificato sui dati: vietano al passeggero di avere l'email del pilota. Sui dati reali condividere la casella è **prassi legittima** (moglie e marito che si iscrivono insieme, e uno solo lascia l'indirizzo). Se qualcuno li collegasse credendo di chiudere un buco, **vieterebbe il caso reale**. | **RISOLTO** il 2026-08-21, dopo averli misurati uno per uno — non rimossi alla cieca. Erano **nove**, non quattro. `ValidateTitolo` e `ValidateSesso`: superati (il titolo è una FK obbligatoria, il sesso lo impone un trigger). `ValidateIndirizzoResidenza` e `ValidateDataNascita`: la regola è ora nel DB (`542` e `fn_ana_clienti_valida`). `ValidatePassengerEmailDifferentFromPilot` e `ValidatePassengerEmailUnique`: **codificavano il contrario del reale** e sono stati eliminati, non collegati. `ValidateTipoDocumento`, `ValidateNumeroDocumento`, `ValidateDocumentoRilasciatoDa`: pretendevano documento obbligatorio — misurato su PROD, **572 clienti su 777 non hanno il tipo documento**, 574 il numero, 579 l'ente. Collegarli avrebbe dichiarato impossibili tre quarti dell'anagrafica. Un obbligo mai applicato non si "ricollega": se lo si vuole, è una decisione, e la sua sede è il database. |
| F | **Sito di iscrizione — non può inviare email** | All'avvio il sito registra `Errore configurazione Mail da DB: operator does not exist: bytea ->> unknown`. La causa è `fn_wizard_get_smtp_config`, che legge i segreti SMTP con `password_enc->>'value'`: li tratta come JSON, ma la cifratura (commit `f056469`) ha reso quelle colonne `bytea`. La funzione non è stata aggiornata perché è una **seconda copia**: quella canonica, usata dal gestionale, è `fn_get_smtp_config_for_email(azienda, master)` e decifra correttamente. Finché resta così, **dal sito non parte nessuna email** — conferme di iscrizione comprese. | **RISOLTO** il 2026-08-20. Il sito legge ora la configurazione tramite `AziendaDAO.get_smtp_config`, che chiama `fn_get_smtp_config_for_email` — la funzione del gestionale. Guadagno collaterale: usa anche `from_name` e `from_email` dell'azienda, due colonne che il codice vecchio ignorava spedendo sempre dall'utenza SMTP. **Resta un passo operativo**: `GV_SECRET_KEY` va aggiunta all'ambiente del processo Flask (in locale a `.env.local`), altrimenti la posta resta spenta — con un avviso esplicito, non più con un errore incomprensibile. |
| G | **Sito — la verifica del codice fiscale non si applica a chi si iscrive per la prima volta** | `CodiceFiscaleValidator.jsx` chiama `/api/validate-cf` passando `client_id`, e l'endpoint verifica il codice **contro un cliente già a database** (`fn_cf_verifica_cliente`). Per un visitatore nuovo `client_id` è nullo: il sito controlla allora soltanto che siano 16 caratteri della forma giusta, e risponde *«non confrontato con l'anagrafica»*. Tutta la verifica costruita negli script `544`–`555` — corrispondenza con i dati, omocodia, **cognome e nome invertiti** — non viene mai raggiunta proprio nel momento in cui servirebbe: la prima iscrizione, quando la persona sta digitando i propri dati. | **RISOLTO** il 2026-08-20. Senza `client_id` l'endpoint usa ora `fn_cf_verifica` sui dati digitati (il sesso derivato dal titolo), e il componente React glieli passa. Verificato: `TLONTN77M24G113N` con cognome e nome scambiati restituisce `CONFERMA` proponendo lo scambio. Insieme, il sito ha ricevuto **tutti** i controlli del gestionale tramite il nuovo `/api/cliente/valida`, che chiama la stessa `fn_ana_clienti_valida`. |
| H | **Avviso nome/sesso — esisteva solo nel gestionale** | La regola che segnala «SIG. + FRANCESCA» viveva unicamente in `CoerenzaNomeSessoValidator.cs`, con la lista dei nomi maschili in -a scritta nel codice. Chi si registrava dal sito non aveva quel controllo: la stessa persona, due comportamenti diversi a seconda della porta da cui entrava. | **RISOLTO** il 2026-08-20 (`SqlScripts/561`, `562`). La regola è `fn_nome_sesso_avviso`, la lista è la tabella **globale** `ana_nomi_maschili_in_a` — modificabile senza rilasciare software — ed è inclusa in `fn_ana_clienti_valida`, quindi arriva a entrambi. Il validatore C# è stato **eliminato**: non erano due copie da allineare, era una copia in più. Nel gestionale l'avviso si ricalcola alla scelta del titolo e all'uscita dal campo Nome. |

---

### Note operative emerse dai test
- Il nuovo controllo del punto 6 fa emergere **anomalie già presenti nei dati**: sul database di sviluppo ha segnalato **9 partenze concluse ma non spuntate come effettuate**. Non è un difetto del programma: sono dati di anagrafica da verificare caso per caso (o il viaggio è stato fatto e manca la spunta, o non è stato realizzato). La correzione si fa nella scheda **Date del viaggio**.
- I bug **9** e **10** hanno la stessa radice: i controlli sulle date erano tutti **relativi** (una data rispetto all'altra, la durata rispetto all'anagrafica) e nessuno **assoluto**. Un refuso sull'anno sposta entrambe le date insieme e resta invisibile. Da qui la regola aggiunta in `Documents/Gestione_check.md`: ogni campo data operativo vuole almeno un controllo di **plausibilità**, non solo confronti fra campi.
- I **test SMTP** (test connessione, invio) vanno eseguiti con **VPN disattivata**: molti server di posta bloccano gli IP di VPN/datacenter sulle porte mail pur lasciando aperte quelle web. Il messaggio d'errore ora lo segnala esplicitamente.

---

*Documento da completare/rivedere fino al go-live. Per la sequenza tecnica di deploy in produzione vedi `Estensione Progetto WEB/Documenti/2026-07-10-Checklist_Go_Live_PROD.md`.*
